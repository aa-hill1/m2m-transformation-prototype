package org.example.app.transformModel.stm;

/**
 * Enum that represents the three type of RoboChart junction: default, initial and probabilistic.
 */
public enum JunctionType {
    DEFAULT,
    INITIAL,
    PROB
}
